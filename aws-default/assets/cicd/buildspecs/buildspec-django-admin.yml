version: 0.2

# Django Backend BuildSpec for AWS CodePipeline
# Repository: Terruvim/demo-be
# This buildspec assumes the Django project structure is at the root of the repository

phases:
  pre_build:
    commands:
      - echo "Starting Django Backend build..."
      - echo "Build started on $(date)"
      - echo "AWS Region $AWS_DEFAULT_REGION"
      - echo "Current directory $(pwd)"
      - echo "Repository contents:"
      - ls -la
      
      # Login to ECR
      - echo "Logging in to Amazon ECR..."
      - aws ecr get-login-password --region $AWS_DEFAULT_REGION | docker login --username AWS --password-stdin $AWS_ACCOUNT_ID.dkr.ecr.$AWS_DEFAULT_REGION.amazonaws.com
      
      # Set image tag
      - COMMIT_HASH=$(echo $CODEBUILD_RESOLVED_SOURCE_VERSION | cut -c 1-7)
      - IMAGE_TAG=${COMMIT_HASH:-latest}
      - REPOSITORY_URI=$AWS_ACCOUNT_ID.dkr.ecr.$AWS_DEFAULT_REGION.amazonaws.com/$ECR_REPO_NAME
      
      - echo "Repository URI $REPOSITORY_URI"
      - echo "Image tag $IMAGE_TAG"
      - echo "Container name $CONTAINER_NAME"

  build:
    commands:
      - echo "Building Django Docker image..."
      - echo "Checking for Dockerfile..."
      - ls -la Dockerfile || echo "Dockerfile not found in root"
      
      # Build Docker image (assuming Dockerfile is in repository root)
      - |
        docker build \
          --build-arg DB_HOST=$DB_HOST \
          --build-arg DB_PORT=$DB_PORT \
          --build-arg DB_NAME=$DB_NAME \
          --build-arg DJANGO_SETTINGS_MODULE=$DJANGO_SETTINGS_MODULE \
          --build-arg DJANGO_SECRET_KEY=$DJANGO_SECRET_KEY \
          -t $REPOSITORY_URI:$IMAGE_TAG \
          -t $REPOSITORY_URI:latest \
          -f Dockerfile .
      
      - echo "Docker image built successfully"
      - docker images | grep $ECR_REPO_NAME

  post_build:
    commands:
      - echo "Pushing Docker image to ECR..."
      - docker push $REPOSITORY_URI:$IMAGE_TAG
      - docker push $REPOSITORY_URI:latest
      
      # Create imagedefinitions.json for ECS deployment
      - echo "Creating image definitions..."
      - printf '[{"name":"%s","imageUri":"%s"}]' $CONTAINER_NAME $REPOSITORY_URI:$IMAGE_TAG > imagedefinitions.json
      
      - echo "Image definitions content:"
      - cat imagedefinitions.json
      
      - echo "Build completed on $(date)"

artifacts:
  files:
    - imagedefinitions.json
  discard-paths: yes

cache:
  paths:
    - '/root/.cache/pip/**/*'
    - '/root/.cache/docker/**/*'
